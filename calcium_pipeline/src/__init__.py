"""
Calcium Pipeline — Contour-based detection for calcium imaging.

Detects structures via blob seeding + contour fitting, extracts fluorescence
traces, deconvolves calcium transients, and runs group-level genotype comparisons.

Typical usage is via the shell entry point (run.sh).
"""

__version__ = "2.1.0"
